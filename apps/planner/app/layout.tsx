import "../../shared/styles.css";
export const metadata = { title: "Hyderabad City Lab | planner", description: "Local planning demonstration with historical and explicitly simulated data." };
export default function Layout({children}: {children:React.ReactNode}) { return <html lang="en"><body>{children}</body></html>; }
