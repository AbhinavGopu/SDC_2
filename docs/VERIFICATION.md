# Verification record

Performed locally on Windows on 2026-09-14. The new root-level Compose stack was left running. The older hyderabad-traffic-agent scaffold was not used for these checks.

| Check | Actual result |
|---|---|
| Backend tests, .venv/Scripts/python.exe -m pytest -q | 8 passed; two upstream Starlette/AnyIO deprecation warnings |
| Citizen Next.js production build | Passed compilation, TypeScript checks and static page generation |
| Planner Next.js production build | Passed compilation, TypeScript checks and static page generation |
| npm install audit | 0 vulnerabilities reported at installation time |
| PostgreSQL/PostGIS/pgvector image | Built |
| FastAPI image | Built |
| SUMO/TraCI/FFmpeg image | Built |
| Citizen and planner container images | Built |
| Running Compose services | API, database, Redis, SUMO worker, citizen and planner all running |
| API/database/Redis/worker health | Healthy; /health reported postgresql, redis=true, sumo_worker=true |
| Database extensions | PostGIS 3.6.4 and vector 0.8.6 queried from pg_extension |
| Actual PostGIS lookup | Point 78.445 E, 17.435 N resolved to demonstration area 1 |
| Actual pgvector operator | Orthogonal vectors [1,0,0] and [0,1,0] returned cosine distance 1 |
| Historical download | Seven daily ERA5 precipitation records fetched, raw response and provenance saved |
| Actual PostgreSQL-backed integration smoke | Passed historical/simulated provenance, offline planning and queued SUMO completion |
| Real SUMO run | Job 3cf36f3d-4491-478a-be11-d6e7d280a58b completed via SUMO/TraCI, 60 seconds, seed 42; baseline and trial both had 4 arrivals (no improvement was fabricated) |
| Chromium end-to-end | 3 passed after the login/top-navigation update: report workflow, citizen login/navigation/sign-out, and planner login/navigation/sign-out, including mobile layout checks |

The browser test initially timed out on an overly strict label selector after completing report submission/review. It was corrected to use the combobox role/name and passed on rerun. A Docker mkdir command and Windows SQLite test teardown were also fixed before the passing results above.

## Latest interface changes

Both portals use the light-blue theme. Logged-out pages show only authentication controls, with empty email/password fields and no displayed demo credentials. Workspace navigation is across the top after login and is removed on sign-out. Both updated production builds passed. All six Compose services were running at the final health check; PostgreSQL, Redis, and the SUMO worker were healthy.

## Evidence and reproduction

Cleanup note: generated browser reports, screenshots under apps/test-results, local build outputs, and installed host dependencies were removed during pre-container cleanup. Browser artifacts regenerated during subsequent UI checks were also removed at final cleanup. The recorded results remain historical evidence; regenerate browser artifacts using the documented test commands after reinstalling development dependencies. Container images and persistent data volumes were retained.

- Backend tests: tests/test_api.py
- Browser test: apps/tests/flows.spec.ts
- Compose smoke test: scripts/smoke.py
- Container build logs: artifacts/backend-build.log and artifacts/frontend-build.log
- Browser report (regenerated by tests): apps/playwright-report/index.html
- Screenshots are generated in test-results relative to the test command working directory; they are not required for container startup.

Browser test reports remain in the development database with names beginning "Browser acceptance test". The integration planning record is also explicitly named as a test.

## What these results do not establish

- No credentialed OpenAI response, embedding generation/retrieval, image analysis or Whisper transcription was exercised. No API key was provided. The offline RAG branch and mocked provider-error fallback passed.
- The vector SQL test verifies the extension/operator; it does not prove live OpenAI embedding ingestion.
- Unit media tests validate upload format, storage/access authorization and queue creation. Credentialed video/vision analysis and audio transcription remain unverified.
- The SUMO scenario is synthetic and uncalibrated. These runs establish technical execution, not real-world effectiveness, an optimal timing plan or reliable emissions predictions.
- No official GHMC boundaries, actual traffic history, utility meters, pollution feeds, right-of-way data or schedule of rates are supplied.
- Browser checks cover a focused desktop workflow and 390 px mobile login/navigation layouts; they are not exhaustive cross-browser, accessibility, load or security certification.
- Production migrations, identity-provider provisioning, recovery/backup drills, secrets management and external notification providers are not configured.

See DATA_AND_EXTERNALS.md for required credentials, attribution and dataset inputs.

## Traffic-dot playback verification

Both frontend production builds passed after adding the SUMO traffic-dot maps. The three existing browser tests passed. The new SUMO browser test passed after fixing named TraCI connection cleanup: SVG coordinates matched the actual job response, stopped-dot counts matched the sampled speeds, and seeking, play/pause, login gating and mobile width checks passed. Repeated 60-second runs with identical inputs produced identical outputs; baseline and trial with equal green times also matched. These checks used actual containerized SUMO, not mocked trajectories.

The maps render recorded frames at ten-second intervals and show at most 100 vehicles per frame. Speed colors indicate vehicle motion, not a validated real-world congestion diagnosis. No map data appears before a run completes. Historical verification statements above remain as dated records.

## Complaint-priority verification

API and both frontend images rebuilt successfully. Twenty backend tests passed in an isolated SQLite test database inside the API container, including accidents, emergency access, imminent/future processions, basic negation, past incidents, severity ordering and moving resolved reports last. The dedicated Chromium priority test passed after correcting a missing badge: actual API-created accident/procession reports showed the expected badges and reasons, in critical-before-high-before-medium order. These rules triage reported text; they do not verify incidents.

## Priority-focused advisor verification

API and both frontend builds passed. Twenty-two backend tests passed, including selecting the most urgent unresolved report, advancing after resolution, preserving budget filtering and immediate review guidance, and handling an empty queue. A dedicated Chromium check passed against the running stack: the brief had an empty value and placeholder-only guidance, the request transmitted no default prompt text, and the result targeted the highest-priority complaint.
